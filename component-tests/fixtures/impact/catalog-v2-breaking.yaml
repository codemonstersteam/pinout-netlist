openapi: 3.0.3
info: {title: catalog, version: "2.0.0"}
paths:
  /items:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  sku: {type: string}
                  qty: {type: integer}
